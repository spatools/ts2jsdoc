<?js
var data = obj;
var self = this;
?>
<?js data.forEach(function(name, i) { ?>
    <span class="param-type text-info"><?js= self.linkto(name, self.htmlsafe(name)) ?></span>
    <?js if (i < data.length - 1) { ?>|<?js } ?>
<?js }); ?>
